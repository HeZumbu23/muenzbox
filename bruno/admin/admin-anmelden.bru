meta {
  name: Admin Anmelden
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/admin/verify
  body: json
  auth: none
}

body:json {
  {
    "pin": "1234"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("adminToken", res.body.token);
  }
}
